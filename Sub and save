openapi: 3.0.0
info:
  title: Subscribe and save
  description: Api that subscribes then saves
  version: 0.1.9

servers:
  - url: http://api.Subscribe.com/v1
    description: The thing you are subscibeing to
  - url: http://Netflix.example.com
    description: Another subscribtion based website

paths:
  /put:
    get:
      summary: adds your subscription
      description: Optional extended description in CommonMark or HTML.
      responses:
        "200": # status code
          description: A JSON array of user names
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
